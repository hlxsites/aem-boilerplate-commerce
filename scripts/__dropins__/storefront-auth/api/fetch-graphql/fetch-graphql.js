import { FetchGraphQL } from '@adobe/fetch-graphql';
export const { setEndpoint, setFetchGraphQlHeader, removeFetchGraphQlHeader, setFetchGraphQlHeaders, fetchGraphQl, getConfig, } = new FetchGraphQL().getMethods();
