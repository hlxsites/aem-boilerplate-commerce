describe("Verify B2B Quote feature", () => {
    it("Verify B2B Quote feature on Cart", { tags: ["@skipSaas", "@skipPaas", "@B2BPaas"] }, () => {
        // Visit the homepage
        cy.visit("/");
        
        // WIP
    });
});
